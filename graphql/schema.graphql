type Item {
  id: Int!
  title: String!
  description: String!
  image: String!
  price: Float!
  category: String!
  amountInCart: Int!
}

input ItemsParams {
  ids: [Int!]
}

type CartItem {
  id: Int!
  item: Item!
  quantity: Int!
  total: String!
}

type Cart {
  items: [CartItem!]!
  total: String!
}

type Category {
  title: String!
  items: [Item!]!
}

type Query {
  items(params: ItemsParams): [Item!]!
  cart: Cart!
  categories: [Category!]!
  category(title: String!): Category
}

input AddToCartParams {
  id: Int!
  quantity: Int!
}

type Mutation {
  addToCart(params: AddToCartParams): CartItem!
}
