query FastItems($page: Int!, $size: Int!) {
  items(paging: { page: $page, size: $size }) {
    content {
      id
      title
      description
      category
      image
      amountInCart
    }
  }
}
