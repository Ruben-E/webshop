import { ArgsTable, Canvas, Meta, Story } from "@storybook/addon-docs/blocks";
import { Home } from "./home";
import { ITEMS_MOCK } from "../../../mocks/items.mock";
import { AppLayout } from "../../../pages/_app";

<Meta
  title={"Pages/Home"}
  component={Home}
  parameters={{
    layout: "fullscreen",
    docs: {
      inlineStories: false,
    },
  }}
/>

## API

<ArgsTable component={Home} />

## Stories

### Loaded 2 results

export const noop = () => {};

<Canvas>
  <Story name="2 items" height={"260px"}>
    <AppLayout currentRoute={"/"}>
      <Home
        addToCart={noop}
        cart={{}}
        itemsState={{ loading: false, data: ITEMS_MOCK.slice(0, 2) }}
      />
    </AppLayout>
  </Story>
</Canvas>

### Loaded 2 results

<Canvas>
  <Story name="6 items" height={"260px"}>
    <AppLayout currentRoute={"/"}>
      <Home
        addToCart={noop}
        cart={{}}
        itemsState={{ loading: false, data: ITEMS_MOCK }}
      />
    </AppLayout>
  </Story>
</Canvas>

### Loading

<Canvas>
  <Story name="Loading" height={"260px"}>
    <AppLayout currentRoute={"/"}>
      <Home addToCart={noop} itemsState={{ loading: true }} cart={{}} />
    </AppLayout>
  </Story>
</Canvas>

### Error

<Canvas>
  <Story name="Error" height={"260px"}>
    <AppLayout currentRoute={"/"}>
      <Home
        addToCart={noop}
        itemsState={{ error: new Error("Something went wrong") }}
        cart={{}}
      />
    </AppLayout>
  </Story>
</Canvas>
