import { ArgsTable, Canvas, Meta, Story } from "@storybook/addon-docs/blocks";
import { HomePage } from "./home";
import { AppLayout } from "../../../pages/_app";
import { ITEMS } from "../../../data";

<Meta
  title={"Pages/Home"}
  component={HomePage}
  parameters={{
    layout: "fullscreen",
    docs: {
      inlineStories: false,
    },
  }}
/>

## API

<ArgsTable component={HomePage} />

## Stories

### Loaded 2 results

export const noop = () => {};
export const CLIENT_ITEMS = ITEMS.map((item) => ({ ...item, inCart: false }));

<Canvas>
  <Story name="2 items" height={"480px"}>
    <AppLayout currentRoute={"/"}>
      <HomePage
        addToCart={noop}
        itemsState={{ loading: false, data: CLIENT_ITEMS.slice(0, 2) }}
      />
    </AppLayout>
  </Story>
</Canvas>

### Loaded 2 results

<Canvas>
  <Story name="6 items" height={"480px"}>
    <AppLayout currentRoute={"/"}>
      <HomePage
        addToCart={noop}
        itemsState={{ loading: false, data: CLIENT_ITEMS }}
      />
    </AppLayout>
  </Story>
</Canvas>

### Cart Loading

<Canvas>
  <Story name="Cart loading" height={"480px"}>
    <AppLayout currentRoute={"/"}>
      <HomePage addToCart={noop} itemsState={{ loading: false, data: ITEMS }} />
    </AppLayout>
  </Story>
</Canvas>

### Loading

<Canvas>
  <Story name="Loading" height={"480px"}>
    <AppLayout currentRoute={"/"}>
      <HomePage addToCart={noop} itemsState={{ loading: true }} />
    </AppLayout>
  </Story>
</Canvas>

### Error

<Canvas>
  <Story name="Error" height={"480px"}>
    <AppLayout currentRoute={"/"}>
      <HomePage
        addToCart={noop}
        itemsState={{ error: new Error("Something went wrong") }}
      />
    </AppLayout>
  </Story>
</Canvas>
