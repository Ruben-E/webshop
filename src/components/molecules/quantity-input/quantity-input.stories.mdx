import { ArgsTable, Canvas, Meta, Story } from "@storybook/addon-docs/blocks";
import { QuantityInput } from "./quantity-input";
import styled from "styled-components";

<Meta title={"Molecules/QuantityInput"} component={QuantityInput} />

## API

<ArgsTable component={QuantityInput} />

## Stories

export const noop = () => {};

export const StyledStory = styled.section`
  h2 {
    margin: 8px 0;
  }
  input {
    margin-bottom: 8px;
  }
`;

### Default

<Canvas>
  <Story name="Default">
    <StyledStory>
      <h2>Zero</h2>
      <QuantityInput onChange={noop} quantity={0} />
      <h2>One (min)</h2>
      <QuantityInput onChange={noop} quantity={1} />
      <h2>Two (above min)</h2>
      <QuantityInput onChange={noop} quantity={2} />
      <h2>Disabled</h2>
      <QuantityInput onChange={noop} quantity={2} disabled />
    </StyledStory>
  </Story>
</Canvas>

### Controlled

<Canvas>
  <Story name="Controlled" args={{ quantity: 0 }}>
    {(args) => (
      <StyledStory>
        <QuantityInput {...args} />
      </StyledStory>
    )}
  </Story>
</Canvas>
